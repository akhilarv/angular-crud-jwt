export class Birthday {
    constructor(
        public id: number,
        public firstName: string,
        public lastName: string,
        public dob: Date,
        public relation: string
    ) { }
}
